server:
  port: 9000

spring:
  application:
    name: mall-gateway

  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.102:8848

    gateway:
      routes:
        - id: user-service
          uri: lb://mall-user
          predicates:
            - Path=/user/**